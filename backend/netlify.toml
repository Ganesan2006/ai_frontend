# In your root netlify.toml
[build]
  # ... other build settings

[functions]
  # Directory where your serverless functions are stored
  directory = "netlify/functions/"

# You can specify the Python version for your functions
[functions."*"]
  runtime = "python3.12"
